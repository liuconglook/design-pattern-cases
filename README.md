# design-pattern-cases
设计模式案例

- acyclic：无环（acyclic visitor）模式
- decorator：装饰器（decorator）模式
- extention：扩展对象（extension object）模式
- report：访问者（visitor）模式
- visitor：访问者（visitor）模式
- state：有限状态机（finite state machine，FSM）模式

